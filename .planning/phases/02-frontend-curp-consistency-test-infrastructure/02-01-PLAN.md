---
phase: 02-frontend-curp-consistency-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - package.json
  - client/vitest.config.ts
  - client/src/test-utils/setup.ts
  - client/src/utils/mask.test.ts
  - client/src/utils/mask.ts
autonomous: true
requirements: [CURP-03, CURP-04, CURP-06]
must_haves:
  truths:
    - "The CURP rendered on the confirmation screen (Confirmacion.tsx) masks all but the first 4 characters, matching the already-correct server-side enmascararCurp (server/src/utils/mask.js)."
    - "`npm --prefix client test` runs Vitest, discovers co-located client/src/**/*.test.ts(x) files, and reports pass/fail."
    - "A regression test for enmascararCurp asserts an exact-equality happy-path result that fails if masking reverts to exposing 5 characters (slice(0,5))."
    - "enmascararCurp's visible-prefix/masked-suffix boundary sits exactly at position 4 -- the 4th character is visible and the 5th onward is masked with asterisks; verified by an exact-equality test against a full 18-char CURP fixture."
    - "enmascararCurp('') returns '' via the existing `if (!curp) return ''` early-return branch; verified by test."
    - "enmascararCurp is a pure string-slicing function that performs no reordering -- the visible prefix retains its original character order in the output; covered by the same exact-equality happy-path test as the adjacency case."
    - "The masked CURP value is always <=18 characters (4 visible + <=14 asterisks) and renders in `.resumen__fila dd`, which already has overflow-wrap: anywhere; reducing visible characters from 5 to 4 introduces no new overflow risk (UI-SPEC long-text/overflow, covered)."
    - statement: "enmascararCurp assumes well-formed ASCII CURP input (18 single-byte alphanumeric characters per the RENAPO CURP format); behavior for non-ASCII/multi-byte input is not explicitly tested since upstream form validation (the phases.config.json pattern) is expected to reject non-ASCII before this function is reached."
      verification: backstop
  artifacts:
    - client/vitest.config.ts
    - client/src/test-utils/setup.ts
    - client/src/utils/mask.test.ts
    - client/src/utils/mask.ts
    - client/package.json
  key_links:
    - "enmascararCurp is imported by client/src/components/wizard/phases/Confirmacion.tsx -- the sole render site of the masked CURP on the confirmation screen."
    - "Vitest discovers tests via client/vitest.config.ts; client/src/test-utils/setup.ts registers @testing-library/jest-dom matchers and lives under src/ so tsconfig.json include:['src'] picks it up."
  prohibitions:
    - "Must not widen CURP masking exposure while fixing the visible-character count: the fix must reduce exposure from 5 visible characters to 4, never increase it, and must not alter enmascararTelefono, enmascararCorreo, or iniciales in the same file."
---

<objective>
Stand up the client's first test runner (Vitest + Testing Library, per CURP-06) and use it to fix and lock the CURP masking over-exposure bug (CURP-03) with a regression test (CURP-04). This is a complete vertical slice: after this plan, `npm --prefix client test` executes and the confirmation screen no longer leaks the 5th CURP character.

Purpose: Patients' partial CURP shown on the confirmation summary must expose only the first 4 characters (matching the server), and the client must have a runnable test suite so this cannot silently revert.
Output: Working Vitest config + jest-dom setup + client `test` script, corrected `enmascararCurp`, and a passing `mask.test.ts` regression test that fails against the old slice(0,5) behavior.
</objective>

## Phase Goal (user story)

**As a** patient completing pre-registration, **I want to** see only the first 4 characters of my CURP on the confirmation screen and have format mistakes caught before I submit, **so that** my personal data is not over-exposed on screen and I can correct errors before they reach the backend.

> Derivation note: this phase's ROADMAP `**Goal:**` line is written in outcome form ("...so that PII isn't over-exposed on screen and format errors surface before submission...") rather than the bolded `As a / I want to / so that` template. The story above is a faithful reformat of that goal (role = patient, from the project domain; outcome quoted directly), not an invented story. Flagged for the checker; run `/gsd mvp-phase 02` if a canonical mvp-phase-authored story is required.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-CONTEXT.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-PATTERNS.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stand up Vitest + Testing Library and author the failing enmascararCurp regression test</name>
  <files>client/package.json, package.json, client/vitest.config.ts, client/src/test-utils/setup.ts, client/src/utils/mask.test.ts</files>
  <read_first>
    - client/package.json (current scripts + devDependencies; add only, do not remove)
    - package.json (root; D-01 -- the root `"test"` script must keep meaning `npm --prefix server test`)
    - client/tsconfig.json (include:['src'] governs whether setup.ts and *.test files are typechecked)
    - client/vite.config.ts (structural style reference only; must NOT be edited or merged into vitest.config.ts)
    - server/tests/arco.test.js (masking assertion precedent: fixture 'PEGG850312MDFRRR04' -> 'PEGG' + '*'.repeat(14))
    - server/tests/curp.test.js (arrange-act-assert style, terse comments on why fixtures are edge/invalid)
    - client/src/utils/mask.ts (the function under test; enmascararCurp lines 3-6, still buggy at this point)
    - .planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md (Pitfall 1 version pin; Pattern 2 config shape)
  </read_first>
  <behavior>
    - happy path (anti-regression): enmascararCurp('PEGG850312MDFRRR04') === 'PEGG' + '*'.repeat(14). This is the ONLY case distinguishing the bug (slice(0,5)) from the fix (slice(0,4)); it must FAIL now.
    - empty: enmascararCurp('') === '' (exercises the `if (!curp) return ''` branch).
    - documented hardening (explicitly labeled NOT anti-regression, because slice(0,5) and slice(0,4) produce identical output when length <= 4): enmascararCurp('PEGG') === 'PEGG'; enmascararCurp('PE') === 'PE'.
  </behavior>
  <action>
    Install the client dev dependencies via `npm --prefix client install -D` at the RESEARCH-verified pins (Pitfall 1: do NOT install latest vitest 4.x -- it requires Vite 6+): vitest@^3.2.7, happy-dom@^20.10.6, @testing-library/react@^16.3.2, @testing-library/user-event@^14.6.1, @testing-library/jest-dom@^6.9.1. Add a `"test": "vitest run"` script to client/package.json scripts (leave dev/build/preview untouched). In root package.json add a NEW convenience script `"test:client": "npm --prefix client test"` (D-01 discretion on the name) and do NOT change the existing root `"test"` key's value. Create client/vitest.config.ts as a standalone config (RESEARCH Pattern 2): import defineConfig from 'vitest/config' and react from '@vitejs/plugin-react'; export plugins [react()] and test block with environment 'happy-dom' (D-02, locked), setupFiles ['./src/test-utils/setup.ts'], globals false (explicit-import style, no-globals convention). Create client/src/test-utils/setup.ts (co-located under src/ per D-03) whose only line imports '@testing-library/jest-dom/vitest' (must live under src/ so tsconfig include:['src'] typechecks the jest-dom matcher augmentation -- Pitfall 5). Create client/src/utils/mask.test.ts co-located next to mask.ts (D-03) importing describe/it/expect from 'vitest' and enmascararCurp from './mask', encoding the four cases from the behavior block; put the two length<=4 cases inside a nested describe explicitly commented as documented hardening, NOT anti-regression coverage (D-06). Do NOT yet modify mask.ts -- the happy-path assertion must be RED at the end of this task, proving the runner executes real assertions.
  </action>
  <verify>
    <automated>npm --prefix client test 2>&1 | grep -Eiq "(FAIL|1 failed|× .*primeros 4|✗)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix client test` invokes Vitest 3.x, loads client/vitest.config.ts, and executes client/src/utils/mask.test.ts (proves the runner is operational).
    - The happy-path test `enmascararCurp('PEGG850312MDFRRR04') === 'PEGG' + '*'.repeat(14)` FAILS against the current slice(0,5) implementation (RED confirmed).
    - The empty-string and two documented-hardening tests are present; the length<=4 cases are inside a describe block whose text marks them as NOT anti-regression coverage.
    - Root package.json `"test"` still resolves to `npm --prefix server test` (unchanged); a new `"test:client"` script exists.
    - `npm --prefix client run build` still runs `tsc --noEmit` without new type errors from setup.ts or mask.test.ts.
  </acceptance_criteria>
  <done>Vitest runner operational; mask.test.ts present with the four D-06 cases; happy-path assertion is RED; root test contract untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix enmascararCurp to expose 4 characters and turn the regression test GREEN</name>
  <files>client/src/utils/mask.ts</files>
  <read_first>
    - client/src/utils/mask.ts (change ONLY enmascararCurp; leave enmascararTelefono, enmascararCorreo, iniciales untouched)
    - server/src/utils/mask.js (the already-correct sibling implementation to mirror exactly: slice(0,4) + '*'.repeat(Math.max(0, length-4)))
  </read_first>
  <action>
    In client/src/utils/mask.ts, change enmascararCurp so both the visible-slice count and the asterisk-count subtrahend go from 5 to 4: return curp.slice(0, 4) concatenated with '*' repeated Math.max(0, curp.length - 4) times, keeping the `if (!curp) return ''` guard. This mirrors server/src/utils/mask.js exactly. Do not change the function signature, the file's other exports, or any imports.
  </action>
  <verify>
    <automated>npm --prefix client test</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix client test` passes with all mask.test.ts cases green (happy-path now returns 'PEGG' + 14 asterisks).
    - enmascararCurp('PEGG850312MDFRRR04') === 'PEGG' + '*'.repeat(14); enmascararCurp('') === ''.
    - enmascararTelefono, enmascararCorreo, and iniciales are byte-for-byte unchanged.
    - `npm --prefix client run build` (tsc --noEmit && vite build) passes.
  </acceptance_criteria>
  <done>enmascararCurp exposes exactly 4 characters; the anti-regression happy-path test is GREEN; no other mask.ts export changed.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01 portion)

New files:
- `client/vitest.config.ts` — standalone Vitest config (environment happy-dom, setupFiles, plugins [react()])
- `client/src/test-utils/setup.ts` — Vitest setup entry importing `@testing-library/jest-dom/vitest`
- `client/src/utils/mask.test.ts` — enmascararCurp regression suite (CURP-04)

New npm scripts:
- client `test` → `vitest run`
- root `test:client` → `npm --prefix client test` (new; root `test` unchanged per D-01)

New client devDependencies:
- `vitest@^3.2.7`, `happy-dom@^20.10.6`, `@testing-library/react@^16.3.2`, `@testing-library/user-event@^14.6.1`, `@testing-library/jest-dom@^6.9.1`

Modified source:
- `client/src/utils/mask.ts` — `enmascararCurp` behavior fixed (5 → 4 visible characters)

<assumptions>
Two edge-probe items came back `unresolved` from the specless probe fallback and are surfaced here rather than silently dropped:

- **CURP-04 (unclassified, unresolved):** the fallback could not classify a specific edge category for the masking regression test beyond the four cases already enumerated in D-06. Handling: the D-06 cases (happy-path anti-regression, empty, two documented-hardening length<=4 cases) are treated as the complete, agreed coverage; no additional undiscovered edge is assumed. If execution surfaces a genuinely new masking edge (e.g. whitespace-only input), flag it in the SUMMARY rather than expanding scope silently.
- **CURP-06 (unclassified, unresolved):** the fallback could not classify a specific edge for the test-infrastructure setup itself. Handling: RESEARCH Open Question 1 (shared-file vi.mock hoisting) is the known live risk and is verified in Plan 02 Task 1's smoke test; no other test-infra edge is assumed here.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render → screen (Confirmacion.tsx `<dd>`) | Partial CURP (PII) is written to the DOM for the patient to recognize their submission. |
| npm registry → client/node_modules | New dev dependencies are installed into the build/test toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | enmascararCurp (client/src/utils/mask.ts) rendered via Confirmacion.tsx | medium | mitigate | Reduce visible characters from 5 to 4 (Task 2), matching server precedent; lock with the exact-equality anti-regression test in mask.test.ts (Task 1) so re-exposure of the 5th character fails CI-local `npm --prefix client test`. |
| T-02-SC | Tampering | npm dev-dependency installs (vitest, happy-dom, @testing-library/*) | low | accept | RESEARCH.md `## Package Legitimacy Audit` verified all five packages against the live npm registry (`npm view time.created` + download counts + official repos); the two `SUS` flags (vitest, happy-dom) are documented false positives of the "latest-patch too-new" heuristic. No `[ASSUMED]`/`[SUS]` package requires a blocking human-verify checkpoint. |

No high/critical threats (block-on threshold: high) — nothing blocks execution.
</threat_model>

<verification>
- `npm --prefix client test` passes (mask.test.ts all green after Task 2).
- `npm --prefix client run build` passes (`tsc --noEmit && vite build`) with the new test/config files in scope.
- `npm test` (root) still runs the backend suite unchanged (D-01 contract intact).
- Manual cross-check: the happy-path masked output 'PEGG' + '*'.repeat(14) equals what `server/tests/arco.test.js` asserts for the same fixture.
</verification>

<success_criteria>
- Vitest runnable via `npm --prefix client test`; co-located `client/src/**/*.test.ts` discovered.
- enmascararCurp exposes exactly 4 characters; regression test would fail if reverted to slice(0,5).
- Root `test` script meaning preserved; new `test:client` convenience script added.
</success_criteria>

<output>
Create `.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-01-SUMMARY.md` when done.
</output>
