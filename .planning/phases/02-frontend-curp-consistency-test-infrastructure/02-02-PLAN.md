---
phase: 02-frontend-curp-consistency-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - server/src/config/phases.config.json
  - client/src/test-utils/renderConWizard.tsx
  - client/src/test-utils/renderConWizard.test.tsx
  - client/src/components/wizard/PhaseRenderer.test.tsx
autonomous: true
requirements: [CURP-05, CURP-06]
must_haves:
  truths:
    - "The `curp` field `pattern` in server/src/config/phases.config.json equals the corrected backend CURP_REGEX from Phase 1 (server/src/validation/schemas.js), duplicated as a JSON-double-escaped string (every \\d becomes \\\\d)."
    - "In the rendered PhaseRenderer form, submitting a valid post-2000 CURP (MAPA000115HDFRRLA1) calls guardarFase('datos_personales', { curp: 'MAPA000115HDFRRLA1' }); submitting a malformed CURP renders 'CURP no tiene un formato válido' (role=alert) and does NOT call guardarFase -- i.e. format errors surface before reaching the backend."
    - "renderConWizard mocks the useWizard hook (not the private WizardContext object) so components under test render without a real WizardProvider or network fetch; a smoke test confirms the shared vi.mock applies across importing test files."
    - "The tightened curp pattern reuses the existing `.campo__error` copy ('CURP no tiene un formato válido') and rendering path in Field.tsx (role=alert); only the trigger condition changes, not the rendering path, wording, or styling (UI-SPEC error, covered)."
    - statement: "A lowercase-but-structurally-valid CURP shows the standard 'CURP no tiene un formato válido' message client-side post-fix because validarCampo does not uppercase before testing while the backend does; this is accepted, in-scope behavior (RESEARCH Assumption A2), and the component test explicitly exercises a lowercase CURP to confirm it is intended, not a silent regression (UI-SPEC case-sensitivity edge)."
      verification: backstop
    - statement: "phases.config.json's curp pattern is static, read-only config compiled into a RegExp per render; client-side form validation runs synchronously on submit against a single field with no shared mutable state, so no concurrency-safety property beyond React's normal synchronous render/validate cycle is exercised."
      verification: backstop
  artifacts:
    - server/src/config/phases.config.json
    - client/src/test-utils/renderConWizard.tsx
    - client/src/test-utils/renderConWizard.test.tsx
    - client/src/components/wizard/PhaseRenderer.test.tsx
  key_links:
    - "PhaseRenderer.tsx validarCampo (lines 11-21) compiles campo.pattern via new RegExp(campo.pattern).test(v) -- the sole client-side site applying the curp pattern; it runs only on submit (continuar), not per keystroke."
    - "renderConWizard.tsx does vi.mock('../context/WizardContext') and vi.mocked(useWizard).mockReturnValue(...); PhaseRenderer.test.tsx imports renderConWizard from ../../test-utils/renderConWizard."
    - "server/src/config/phases.config.json curp.pattern must be kept manually in sync with server/src/validation/schemas.js CURP_REGEX (no shared import -- deliberate duplication per PROJECT.md Key Decisions)."
  prohibitions:
    - "The client-side curp pattern must not reject CURPs the backend CURP_REGEX would accept: it must be duplicated verbatim (same character classes, same tail [B-DF-HJ-NP-TV-Z]{3}[A-Z0-9]\\d$), never tightened beyond backend parity. The one pre-accepted divergence is lowercase input (RESEARCH A2); no other client-stricter-than-backend behavior may be introduced."
---

<objective>
Bring the frontend's CURP format validation into parity with the corrected backend regex (CURP-05) and prove it through the actual rendered wizard form, while building the repo's first reusable React Testing Library harness (CURP-06 / D-05). This is a complete vertical slice: after this plan, a malformed CURP is rejected in the form before submission, verified by a passing component test.

Purpose: Format errors must surface client-side (before the network round-trip) instead of only at backend submission, using a pattern string that exactly mirrors the authoritative backend regex.
Output: Corrected `curp.pattern` in phases.config.json, a reusable `renderConWizard` harness (mocking useWizard), a smoke test proving the shared mock applies, and a PhaseRenderer component test covering accept/reject/lowercase-edge.
</objective>

## Phase Goal (user story)

**As a** patient completing pre-registration, **I want to** have a malformed CURP flagged in the form before I submit, **so that** I fix it immediately instead of after a failed backend submission. (See Plan 02-01 for the full derivation note.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-CONTEXT.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-PATTERNS.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-UI-SPEC.md
@.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync phases.config.json curp.pattern to the corrected backend CURP_REGEX</name>
  <files>server/src/config/phases.config.json</files>
  <read_first>
    - server/src/validation/schemas.js (lines 4-5: CURP_REGEX -- the source-of-truth JS regex literal to duplicate)
    - server/src/config/phases.config.json (line 17: current loose curp pattern `^[A-Za-z0-9]{18}$`; note the sibling `telefono` pattern `^\\d{10}$` which already demonstrates the correct JSON double-backslash escaping)
    - .planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md (Pitfall 4: JSON escaping; Pitfall 2/A2: lowercase consequence)
  </read_first>
  <action>
    In server/src/config/phases.config.json, replace ONLY the `curp` field's `pattern` value (line 17) from `^[A-Za-z0-9]{18}$` to the corrected backend regex, copied from schemas.js CURP_REGEX with every regex backslash doubled for JSON (each \d becomes \\d), matching the escaping convention already used by the telefono field in the same file. The resulting string begins `^[A-Z][AEIOUX][A-Z]{2}\\d{2}(0[1-9]|1[0-2])(0[1-9]|[12]\\d|3[01])[HM]`, includes the entidad alternation `(AS|BC|BS|CC|CL|CM|CS|CH|DF|DG|GT|GR|HG|JC|MC|MN|MS|NT|NL|OC|PL|QT|QR|SP|SL|SR|TC|TS|TL|VZ|YN|ZS|NE)`, and ends with the corrected tail `[B-DF-HJ-NP-TV-Z]{3}[A-Z0-9]\\d$`. Do NOT touch curp's maxLength, ayuda, requerido, tipo, or its position in the campos array, and do not touch any other field or fase.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cfg=JSON.parse(fs.readFileSync('server/src/config/phases.config.json','utf8'));const p=cfg.fases.find(f=>f.id==='datos_personales').campos.find(x=>x.nombre==='curp').pattern;const src=fs.readFileSync('server/src/validation/schemas.js','utf8');const m=src.match(/CURP_REGEX\s*=\s*\/(.+?)\/;/s);process.exit(m&&p===m[1]?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The parsed curp.pattern string is exactly equal to `CURP_REGEX.source` from schemas.js (the node verify exits 0), confirming byte-for-byte parity between the duplicated client-facing pattern and the authoritative backend regex.
    - phases.config.json still parses as valid JSON (server boots / `npm test` backend suite unaffected).
    - The valid post-2000 CURP `MAPA000115HDFRRLA1` matches `new RegExp(pattern)`; a malformed CURP like `NOVALIDA123` does not.
    - No other field, fase, or curp attribute (maxLength/ayuda/tipo) is modified.
  </acceptance_criteria>
  <done>curp.pattern equals the corrected backend CURP_REGEX source (double-escaped in JSON); JSON still valid; no other config changed.</done>
</task>

<task type="auto">
  <name>Task 2: Build the reusable renderConWizard harness and a smoke test proving the shared mock applies</name>
  <files>client/src/test-utils/renderConWizard.tsx, client/src/test-utils/renderConWizard.test.tsx</files>
  <read_first>
    - client/src/context/WizardContext.tsx (exports ONLY WizardProvider and useWizard; WizardContext object and WizardContextValor interface are module-private -- do NOT import them; mock the useWizard hook instead)
    - client/src/components/wizard/PhaseRenderer.tsx (consumes useWizard: config, datos, faseActual, guardarFase, irAFase; submit button label 'Guardar y continuar')
    - client/src/types.ts (FaseConfig / CampoConfig shapes for the test fixture)
    - .planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md (Pattern 1 harness; Pitfall 3 ReturnType<typeof useWizard>; Open Question 1 shared-mock verification)
    - client/src/test-utils/setup.ts (existing jest-dom setup from Plan 01)
  </read_first>
  <action>
    Create client/src/test-utils/renderConWizard.tsx (RESEARCH Pattern 1). At module scope call vi.mock('../context/WizardContext', async importOriginal => spread the original module and override useWizard with vi.fn()). Type the mock structurally via `type ContextoWizard = ReturnType<typeof useWizard>` (do NOT import WizardContextValor -- it is unexported, Pitfall 3). Provide a contextoBase(overrides) factory returning the full useWizard shape (config null, datos {}, faseActual 0, maxFase 0, draftId 'draft-test', folio null, cargando false, errorGeneral null, and vi.fn() for iniciar, guardarFase [mockResolvedValue undefined], irAFase, enviar, reiniciar) merged with overrides. Export `renderConWizard(ui, overrides)` that sets vi.mocked(useWizard).mockReturnValue(contextoBase(overrides)) then returns render(ui) from @testing-library/react. Create client/src/test-utils/renderConWizard.test.tsx as a minimal smoke test (RESEARCH Open Question 1): render PhaseRenderer with an isolated single-field curp FaseConfig fixture and an overridden guardarFase vi.fn(), then assert the submit button 'Guardar y continuar' is in the document -- proving the shared-file vi.mock applies to a test file that only imports renderConWizard. If the shared mock does NOT apply, fall back to duplicating the vi.mock('../../context/WizardContext') line directly in each *.test.tsx file (documented fallback, not a redesign).
  </action>
  <verify>
    <automated>npm --prefix client test -- src/test-utils/renderConWizard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The smoke test passes: PhaseRenderer renders under renderConWizard without hitting the real WizardProvider/network, and the 'Guardar y continuar' submit button is found.
    - renderConWizard is exported and typed via ReturnType<typeof useWizard>; no import of WizardContextValor or the raw WizardContext object exists.
    - WizardContext.tsx is unmodified.
    - `npm --prefix client run build` (tsc --noEmit) passes with the new harness files in scope.
  </acceptance_criteria>
  <done>renderConWizard harness exists, is typed without private exports, and a smoke test confirms the shared vi.mock applies across importing test files.</done>
</task>

<task type="auto">
  <name>Task 3: Component test — PhaseRenderer accepts valid CURP, rejects malformed, and documents the lowercase edge</name>
  <files>client/src/components/wizard/PhaseRenderer.test.tsx</files>
  <read_first>
    - client/src/components/wizard/PhaseRenderer.tsx (validarCampo error text `${campo.etiqueta} no tiene un formato válido`; continuar calls guardarFase only when no field errors; submit button 'Guardar y continuar')
    - client/src/components/fields/Field.tsx (per-field error renders as `<p className="campo__error" role="alert">` -- the assertion target)
    - client/src/test-utils/renderConWizard.tsx (harness from Task 2)
    - client/src/types.ts (FaseConfig / CampoConfig)
    - server/tests/curp.test.js (fixtures MAPA000115HDFRRLA1 valid post-2000; reuse for cross-reference)
    - .planning/phases/02-frontend-curp-consistency-test-infrastructure/02-RESEARCH.md (Pattern 1 test; Pitfall 2 happy-dom form-submit fallback; A2 lowercase)
  </read_first>
  <action>
    Create client/src/components/wizard/PhaseRenderer.test.tsx (RESEARCH Pattern 1). Import describe/it/expect/vi from 'vitest', screen from @testing-library/react, userEvent from @testing-library/user-event, renderConWizard from ../../test-utils/renderConWizard, PhaseRenderer, and FaseConfig from ../../types. Define an isolated fixture faseCurpSolo: a form FaseConfig whose only campo is curp (requerido true, maxLength 18, tipo text) with the pattern value set to the SAME corrected regex string written into phases.config.json in Task 1 (JSON-escaped form) -- an isolated single-field fase avoids having to satisfy unrelated required fields (RESEARCH anti-pattern). Write three tests: (1) 'accepts a valid post-2000 CURP' -- render with overridden guardarFase vi.fn().mockResolvedValue(undefined), userEvent.type getByLabelText(/curp/i) with 'MAPA000115HDFRRLA1', userEvent.click the /guardar y continuar/i button, expect guardarFase called with 'datos_personales' and { curp: 'MAPA000115HDFRRLA1' }; (2) 'rejects a malformed CURP before calling guardarFase' -- type 'NOVALIDA123', submit, expect screen.getByRole('alert') toHaveTextContent 'CURP no tiene un formato válido' and guardarFase not called; (3) 'lowercase structurally-valid CURP is rejected client-side (accepted A2 behavior, not a regression)' -- type the lowercase form of the valid fixture ('mapa000115hdfrrla1'), submit, expect the same 'CURP no tiene un formato válido' alert and guardarFase not called, with a comment citing RESEARCH A2 / UI-SPEC case-sensitivity edge that this is intended (validarCampo does not uppercase before testing). If happy-dom's documented form-submit gap causes flakiness (Pitfall 2), fall back in priority order to fireEvent.submit on the form element, then to a per-file `// @vitest-environment jsdom` override (D-02 pre-approved escape hatch).
  </action>
  <verify>
    <automated>npm --prefix client test</automated>
  </verify>
  <acceptance_criteria>
    - All three PhaseRenderer tests pass (valid accepted, malformed rejected, lowercase rejected), plus the mask.test.ts and smoke suites from prior work stay green.
    - The valid case asserts guardarFase('datos_personales', { curp: 'MAPA000115HDFRRLA1' }); the malformed and lowercase cases assert role=alert text 'CURP no tiene un formato válido' and guardarFase NOT called.
    - The lowercase test carries a comment marking it as intended A2 behavior, not a regression.
    - `npm --prefix client run build` passes with the component test in scope.
  </acceptance_criteria>
  <done>PhaseRenderer component test proves client-side CURP pattern validation (accept/reject/lowercase-edge) using the corrected pattern; full client suite green.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02 portion)

New files:
- `client/src/test-utils/renderConWizard.tsx` — exports `renderConWizard(ui, overrides)`; mocks the `useWizard` hook (D-05 reusable harness)
- `client/src/test-utils/renderConWizard.test.tsx` — smoke test verifying the shared vi.mock applies
- `client/src/components/wizard/PhaseRenderer.test.tsx` — CURP pattern-validation component test (CURP-05 verification)

Modified config keys:
- `server/src/config/phases.config.json` → `datos_personales.campos[curp].pattern` now equals the corrected backend `CURP_REGEX` (double-escaped)

New exported helper:
- `renderConWizard(ui: ReactElement, overrides?: Partial<ReturnType<typeof useWizard>>)` from `client/src/test-utils/renderConWizard.tsx`

<assumptions>
- **RESEARCH Open Question 1** (shared-file `vi.mock` hoisting applies across importing test files) is verified live by Task 2's smoke test BEFORE Task 3 builds real assertions on it. Documented fallback if it does not apply: move the `vi.mock('../../context/WizardContext')` line into each `*.test.tsx` file directly (small duplication, no redesign).
- **RESEARCH Assumption A2 / UI-SPEC case-sensitivity edge** (lowercase CURP rejected client-side while backend would accept after its own uppercase transform) is treated as accepted in-scope behavior and is explicitly exercised by Task 3 test (3) — not silently patched. If the user deems this unacceptable, that is a NEW decision (uppercase-before-test or case-insensitive pattern), out of this phase's locked scope.
</assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → API | User-entered CURP crosses from the browser to the backend on submit; client-side pattern validation is a UX pre-check, not a security control. |
| config file → RegExp | phases.config.json's curp.pattern is read and compiled into a RegExp at render time in PhaseRenderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | Client-side curp pattern validation (PhaseRenderer.validarCampo) mistaken for / bypassed as a security boundary | low | accept | The authoritative validation remains the backend Zod `esquemaDatosPersonales` (`.strict()`, CURP_REGEX), fixed in Phase 1 and unchanged here (ASVS V5). A malicious client can always POST arbitrary data; the client pattern is explicitly a UX optimization. No client-side security assumption is introduced. |
| T-02-03 | Denial of Service (usability) | curp pattern tightened stricter than backend, blocking valid patients | low | mitigate | Duplicate the backend regex verbatim (Task 1, node equality verify) so client acceptance == backend acceptance; component test asserts a valid post-2000 CURP is accepted. The single known divergence (lowercase input) is documented, tested, and accepted (A2); the prohibition guards against any further client-stricter behavior. |

No high/critical threats (block-on threshold: high) — nothing blocks execution.

> Canon-referral breadcrumb (prohibition probe): "client-side-validation-as-security-boundary" and "PII-in-logs" concerns are covered by general security practice / RESEARCH.md Security Domain (V5 Input Validation, Tampering) and were NOT minted as phase-specific prohibitions.
</threat_model>

<verification>
- `npm --prefix client test` passes (harness smoke + PhaseRenderer accept/reject/lowercase + Plan 01 mask tests).
- `node` equality check confirms phases.config.json curp.pattern === schemas.js CURP_REGEX.source.
- `npm --prefix client run build` (`tsc --noEmit && vite build`) passes.
- `npm test` (root backend suite) still passes — phases.config.json remains valid JSON.
</verification>

<success_criteria>
- phases.config.json curp.pattern matches the corrected backend regex exactly.
- A malformed CURP is rejected in the rendered form (role=alert 'CURP no tiene un formato válido', guardarFase not called); a valid post-2000 CURP is accepted.
- Reusable renderConWizard harness exists and is proven by a smoke test; no changes to WizardContext.tsx.
</success_criteria>

<output>
Create `.planning/phases/02-frontend-curp-consistency-test-infrastructure/02-02-SUMMARY.md` when done.
</output>
